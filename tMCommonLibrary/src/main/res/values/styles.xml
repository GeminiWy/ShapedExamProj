<resources>

    <!--
        Base application theme, dependent on API level. This theme is replaced
        by AppBaseTheme from res/values-vXX/styles.xml on newer devices.
    -->
    <style name="AppBaseTheme" parent="android:Theme.Light">
        <!--
            Theme customizations available in newer API levels can go in
            res/values-vXX/styles.xml, while customizations related to
            backward-compatibility can go here.
        -->
    </style>

    <!-- Application theme. -->
    <style name="AppTheme" parent="AppBaseTheme">
        <!-- All customizations that are NOT specific to a particular API-level can go here. -->
    </style>
	
	<style name="NoTitleBar" parent="AppBaseTheme">
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowAnimationStyle">@style/ActivityAnimation</item>
    </style>
    
    <style name="ActivityAnimation">
        <item name="android:activityOpenEnterAnimation">@anim/push_pic_left_in</item>
        <!-- 打开activity，设置activity进入展示动画 -->
        <item name="android:activityOpenExitAnimation">@anim/push_pic_left_out</item> 
        <!-- 打开activity，设置上个activity离开动画 -->
        <!-- <item name="android:activityCloseEnterAnimation">@anim/push_pic_right_in</item>  -->
        <!-- 结束activity，设置上个activity进入动画 -->
        <!-- <item name="android:activityCloseExitAnimation">@anim/push_pic_right_out</item> -->
        <!-- 结束activity，设置当前activity离开动画 -->
    </style>
    <style name="ProgressBar_Mini" parent="@android:style/Widget.ProgressBar.Horizontal">
	    <item name="android:maxHeight">50dip</item>
	    <item name="android:minHeight">6dip</item>
	    <item name="android:indeterminateOnly">false</item>
	    <item name="android:indeterminateDrawable">@android:drawable/progress_indeterminate_horizontal</item>
	    <item name="android:progressDrawable">@drawable/progressbar_mini</item>
	</style>
    
</resources>
