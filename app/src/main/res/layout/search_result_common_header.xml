<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
   	android:layout_height="@dimen/_72px"
    android:background="@color/search_title_bg_green"
    android:gravity="center"
    android:paddingBottom="5dp"
    android:paddingTop="5dp" >

    <FrameLayout
        android:layout_width="0dp"
        android:layout_height="@dimen/_46px"
        android:layout_marginLeft="10dp"
        android:layout_weight="8" >

        <EditText
            android:id="@+id/search_all_content_ed"
            android:layout_width="fill_parent"
            android:layout_height="fill_parent"
            android:background="@android:color/background_light"
            android:paddingLeft="35dp"
            android:singleLine="true"
            android:textSize="16sp" 
            android:hint="@string/search_hint_default"/>

        <ImageView
            android:id="@+id/search_all_btn_img"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="5dp"
            android:background="@android:color/background_light"
            android:padding="2dp"
            android:src="@drawable/classroom_icon_search" />
    </FrameLayout>

    <Button
        android:id="@+id/search_all_goback_btn"
        android:layout_width="0dp"
        android:layout_height="@dimen/_46px"
        android:layout_marginLeft="5dp"
        android:layout_marginRight="5dp"
        android:layout_weight="2"
        android:background="@color/search_btn_bg_green"
        android:text="@string/search_cancel"
        android:textColor="@color/white"
        android:textSize="@dimen/_21px" />

</LinearLayout>
